namespace GoncaloMCOliveira.CinemachineCameraShake {
    public enum ShakeLifetime {
        Timed,     // Ends automatically after Duration
        Looping    // Runs until Stop() is called
    }
}

